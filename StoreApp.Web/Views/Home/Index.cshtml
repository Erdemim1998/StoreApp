@model List<Product>

@{
    ViewData["Title"] = "Ana Sayfa";
}

@if (ViewBag.Url == null && ViewBag.qParam == null)
{
    <div class="row">
        <div class="col-12 mb-3">
            <div id="carouselExampleIndicators" class="carousel slide" data-bs-ride="carousel">
                <div class="carousel-indicators">
                    <button type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide-to="0" class="active" aria-current="true" aria-label="Slide 1"></button>
                    <button type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide-to="1" aria-label="Slide 2"></button>
                    <button type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide-to="2" aria-label="Slide 3"></button>
                    <button type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide-to="3" aria-label="Slide 4"></button>
                    <button type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide-to="4" aria-label="Slide 5"></button>
                    <button type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide-to="5" aria-label="Slide 6"></button>
                    <button type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide-to="6" aria-label="Slide 7"></button>
                    <button type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide-to="7" aria-label="Slide 8"></button>
                    <button type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide-to="8" aria-label="Slide 9"></button>
                    <button type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide-to="9" aria-label="Slide 10"></button>
                </div>
                <div class="carousel-inner">
                    <div class="carousel-item active">
                        <img src="/img/Slider 1.png" class="d-block w-100" alt="Slider 1">
                    </div>
                    <div class="carousel-item">
                        <img src="/img/Slider 2.png" class="d-block w-100" alt="Slider 2">
                    </div>
                    <div class="carousel-item">
                        <img src="/img/Slider 3.png" class="d-block w-100" alt="Slider 3">
                    </div>
                    <div class="carousel-item">
                        <img src="/img/Slider 4.png" class="d-block w-100" alt="Slider 4">
                    </div>
                    <div class="carousel-item">
                        <img src="/img/Slider 5.png" class="d-block w-100" alt="Slider 5">
                    </div>
                    <div class="carousel-item">
                        <img src="/img/Slider 6.png" class="d-block w-100" alt="Slider 6">
                    </div>
                    <div class="carousel-item">
                        <img src="/img/Slider 7.png" class="d-block w-100" alt="Slider 7">
                    </div>
                    <div class="carousel-item">
                        <img src="/img/Slider 8.png" class="d-block w-100" alt="Slider 8">
                    </div>
                    <div class="carousel-item">
                        <img src="/img/Slider 9.png" class="d-block w-100" alt="Slider 9">
                    </div>
                    <div class="carousel-item">
                        <img src="/img/Slider 10.png" class="d-block w-100" alt="Slider 10">
                    </div>
                </div>
                <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Previous</span>
                </button>
                <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Next</span>
                </button>
            </div>
        </div>
    </div>
}

@section Header{
    @await Html.PartialAsync("_Header", null, new ViewDataDictionary(ViewData) { { "Authenticated", ViewBag.IsAuthenticated }, { "Admin", ViewBag.IsAdmin }, { "User", ViewBag.UserId } })
}

<div class="row">
    <div class="col-3">
        <div class="card">
            <div class="card-header">Filtrele</div>

            <div class="card-body">
                @* <form id="frmFilter" asp-controller="Home" asp-action="Index" method="post"> *@
                    <div class="card-title">Marka</div>
                    <hr />
                    <div class="card-text">
                        @if (ViewBag.Brands != null)
                        {
                            @foreach (Brand brand in ViewBag.Brands)
                            {
                                <div class="mb-3">
                                    @if (ViewBag.SelectedBrands != null && ViewBag.SelectedBrands.Count > 0)
                                    {
                                        bool selectedBrand = false;

                                        @foreach (int selBrandId in ViewBag.SelectedBrands)
                                        {
                                            if (brand.Id == selBrandId)
                                            {
                                                selectedBrand = true;
                                            }
                                        }

                                        if (selectedBrand)
                                        {
                                            <input type="checkbox" checked id="brand_@brand.Id" name="brands" value="@brand.Id" class="form-check-input" />
                                        }

                                        else
                                        {
                                            <input type="checkbox" id="brand_@brand.Id" name="brands" value="@brand.Id" class="form-check-input" />
                                        }
                                    }

                                    else
                                    {
                                        <input type="checkbox" id="brand_@brand.Id" name="brands" value="@brand.Id" class="form-check-input" />
                                    }

                                    <label for="brand_@brand.Id" class="form-check-label">@brand.Name</label>
                                </div>
                            }
                        }

                        else
                        {
                            @foreach (BrandSubCategory brandSubCategory in ViewBag.BrandSubCategories)
                            {
                                <div class="mb-3">
                                    @if (ViewBag.SelectedBrands != null && ViewBag.SelectedBrands.Count > 0)
                                    {
                                        bool selectedBrand = false;

                                        @foreach (int selBrandId in ViewBag.SelectedBrands)
                                        {
                                            if(brandSubCategory.BrandId == selBrandId)
                                            {
                                                selectedBrand = true;
                                            }
                                        }

                                        if (selectedBrand)
                                        {
                                            <input type="checkbox" checked id="brand_@brandSubCategory.BrandId" name="brands" value="@brandSubCategory.BrandId" class="form-check-input" />
                                        }

                                        else
                                        {
                                            <input type="checkbox" id="brand_@brandSubCategory.BrandId" name="brands" value="@brandSubCategory.BrandId" class="form-check-input" />
                                        }
                                    }

                                    else
                                    {
                                        <input type="checkbox" id="brand_@brandSubCategory.BrandId" name="brands" value="@brandSubCategory.BrandId" class="form-check-input" />
                                    }

                                    <label for="brand_@brandSubCategory.BrandId" class="form-check-label">@brandSubCategory.Brand.Name</label>
                                </div>
                            }
                        }
                    </div>

                    <div class="card-title">Fiyat Aralığı</div>
                    <hr />
                    <div class="card-text">
                        <div class="mb-3">
                            <label for="minPrice" class="form-label">En Düşük Fiyat</label>
                            <input type="number" id="minPrice" name="minPrice" value="@ViewBag.MinPrice" class="form-control" />
                        </div>

                        <div class="mb-3">
                            <label for="maxPrice" class="form-label">En Yüksek Fiyat</label>
                            <input type="number" id="maxPrice" name="maxPrice" value="@ViewBag.MaxPrice" class="form-control" />
                        </div>
                    </div>

                    <input type="hidden" id="url" name="url" value="@ViewBag.Url" />
                    <input type="hidden" id="q" name="q" value="@ViewBag.qParam" />
                    <input type="submit" id="btnFilter" class="btn w-100 mainPageButtons" value="Filtrele" />
                @* </form> *@
            </div>
        </div>
    </div>

    <div class="col-9">
        <h1 class="h4">@(ViewBag.Url != null ? ViewBag.SubCategoryName : (ViewBag.qParam != null ? "'" + ViewBag.qParam + "'" + " ile ilgili " + ViewBag.ProductCount + " ürün bulduk" : "Ürünler"))</h1>
        <hr />
        @if(Model.Count > 0)
        {
            <div class="row">
                @foreach (Product product in Model)
                {
                    <div class="mb-3 col-3">
                        <div class="card prdCards" data-cardid="@product.Id" data-url="@product.Url">
                            <div class="card-img-top">
                                <div id="carouselIndicatorsProducts_@product.Id" class="carousel slide" data-bs-ride="carousel">
                                    <div class="carousel-indicators w-100">
                                        @{
                                            List<ProductImage>? productImages = await DataControl.GetCardProductImages(product.Id);

                                            if (productImages.Count > 1)
                                            {
                                                foreach (ProductImage image in productImages)
                                                {
                                                        @if (image == productImages.First())
                                                    {
                                                            <button type="button" data-bs-target="#carouselIndicatorsProducts_@product.Id" data-bs-slide-to="@(image.Id - 1)" class="active rounded-circle" aria-current="true" aria-label="Slide @image.Id" style="background-color: orange; width: 10px; height: 10px;"></button>
                                                    }

                                                    else
                                                    {
                                                            <button type="button" data-bs-target="#carouselIndicatorsProducts_@product.Id" data-bs-slide-to="@(image.Id - 1)" class="rounded-circle" aria-label="Slide @image.Id" style="background-color: orange; width: 10px; height: 10px;"></button>
                                                    }
                                                }
                                            }
                                        }
                                    </div>
                                    <div class="carousel-inner">
                                        @{
                                            foreach (ProductImage image in productImages)
                                            {
                                                    @if (image == productImages.First())
                                                {
                                                        <div class="carousel-item active">
                                                            <img src="@image.Url" class="d-block w-100" alt="Slider @image.Id">
                                                        </div>
                                                }

                                                else
                                                {
                                                        <div class="carousel-item">
                                                            <img src="@image.Url" class="d-block w-100" alt="Slider @image.Id">
                                                        </div>
                                                }
                                            }
                                        }
                                    </div>

                                    @if (productImages.Count > 1)
                                    {
                                        <button class="carousel-control-prev" type="button" data-bs-target="#carouselIndicatorsProducts_@product.Id" data-bs-slide="prev">
                                            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                                            <span class="visually-hidden">Previous</span>
                                        </button>
                                        <button class="carousel-control-next" type="button" data-bs-target="#carouselIndicatorsProducts_@product.Id" data-bs-slide="next">
                                            <span class="carousel-control-next-icon" aria-hidden="true"></span>
                                            <span class="visually-hidden">Next</span>
                                        </button>
                                    }
                                </div>
                            </div>
                            <div class="card-body">
                                <div class="card-title" style="color:#FF6000">@product.Brand.Name</div>
                                <div class="card-title">@product.Name</div>
                                <div class="mb-3">
                                    <div class="card-text">@product.Price.ToString() TL</div>
                                </div>
                                <input type="submit" id="btnBasket_@product.Id" data-productid="@product.Id" style="display:none;" class="btn w-100 addBasket" value="Sepete Ekle" />
                            </div>
                        </div>
                    </div>
                }
            </div>
        }

        else
        {
            <table class="table table-bordered">
                <tr>
                    <td>Listelenecek bir ürün bulunamadı.</td>
                </tr>
            </table>
        }

        <nav>
            <ul class="pagination justify-content-center">
                @{
                    int PageCount = 0;
                    string brands = ViewBag.SelBrandsStr;
                    float minPrice = ViewBag.MinPrice;
                    float maxPrice = ViewBag.MaxPrice;
                    List<Product> products;
                    List<int> brandIds = new List<int>();

                    if (!string.IsNullOrEmpty(brands))
                    {
                        foreach (string brandId in brands.Split(","))
                        {
                            brandIds.Add(int.Parse(brandId));
                        }
                    }

                    if (string.IsNullOrEmpty(ViewBag.Url))
                    {
                        if (ViewBag.qParam != null)
                        {
                            products = await DataControl.GetProductsBySearchText(ViewBag.qParam);

                            if (string.IsNullOrEmpty(brands) && minPrice == 0 && maxPrice > 0)
                            {
                                products = products.Where(p => p.Price <= maxPrice).ToList();
                            }

                            else if (string.IsNullOrEmpty(brands) && minPrice > 0 && maxPrice == 0)
                            {
                                products = products.Where(p => p.Price >= minPrice).ToList();
                            }

                            else if (string.IsNullOrEmpty(brands) && minPrice > 0 && maxPrice > 0)
                            {
                                products = products.Where(p => p.Price >= minPrice && p.Price <= maxPrice).ToList();
                            }

                            else if (!string.IsNullOrEmpty(brands) && minPrice == 0 && maxPrice == 0)
                            {
                                products = products.Where(p => brandIds.Contains(p.BrandId)).ToList();
                            }

                            else if (!string.IsNullOrEmpty(brands) && minPrice == 0 && maxPrice > 0)
                            {
                                products = products.Where(p => brandIds.Contains(p.BrandId) && p.Price <= maxPrice).ToList();
                            }

                            else if (!string.IsNullOrEmpty(brands) && minPrice > 0 && maxPrice == 0)
                            {
                                products = products.Where(p => brandIds.Contains(p.BrandId) && p.Price >= minPrice).ToList();
                            }

                            else if (!string.IsNullOrEmpty(brands) && minPrice > 0 && maxPrice > 0)
                            {
                                products = products.Where(p => brandIds.Contains(p.BrandId) && p.Price >= minPrice && p.Price <= maxPrice).ToList();
                            }

                            PageCount = Convert.ToInt32(Math.Ceiling((float)products.Count / 8));
                        }

                        else if (string.IsNullOrEmpty(brands) && minPrice == 0 && maxPrice == 0)
                        {
                            products = await DataControl.GetProducts();
                            PageCount = Convert.ToInt32(Math.Ceiling((float)products.Count / 8));
                        }

                        else if (string.IsNullOrEmpty(brands) && minPrice == 0 && maxPrice > 0)
                        {
                            products = await DataControl.GetProductsMaxPrice(maxPrice);
                            PageCount = Convert.ToInt32(Math.Ceiling((float)products.Count / 8));
                        }

                        else if (string.IsNullOrEmpty(brands) && minPrice > 0 && maxPrice == 0)
                        {
                            products = await DataControl.GetProductsMinPrice(minPrice);
                            PageCount = Convert.ToInt32(Math.Ceiling((float)products.Count / 8));
                        }

                        else if (string.IsNullOrEmpty(brands) && minPrice > 0 && maxPrice > 0)
                        {
                            products = await DataControl.GetProductsMinMaxPrice(minPrice, maxPrice);
                            PageCount = Convert.ToInt32(Math.Ceiling((float)products.Count / 8));
                        }

                        else if (!string.IsNullOrEmpty(brands) && minPrice == 0 && maxPrice == 0)
                        {
                            products = await DataControl.GetProductsBrands(brands);
                            PageCount = Convert.ToInt32(Math.Ceiling((float)products.Count / 8));
                        }

                        else if (!string.IsNullOrEmpty(brands) && minPrice == 0 && maxPrice > 0)
                        {
                            products = await DataControl.GetProductsBrandsMaxPrice(brands, maxPrice);
                            PageCount = Convert.ToInt32(Math.Ceiling((float)products.Count / 8));
                        }

                        else if (!string.IsNullOrEmpty(brands) && minPrice > 0 && maxPrice == 0)
                        {
                            products = await DataControl.GetProductsBrandsMinPrice(brands, minPrice);
                            PageCount = Convert.ToInt32(Math.Ceiling((float)products.Count / 8));
                        }

                        else
                        {
                            products = await DataControl.GetProductsBrandsMinMaxPrice(brands, minPrice, maxPrice);
                            PageCount = Convert.ToInt32(Math.Ceiling((float)products.Count / 8));
                        }
                    }

                    else
                    {
                        string url = ViewBag.Url;

                        if(string.IsNullOrEmpty(brands) && minPrice == 0 && maxPrice == 0)
                        {
                            products = await DataControl.GetProducts(url);
                            PageCount = Convert.ToInt32(Math.Ceiling((float)products.Count / 8));
                        }

                        else if (string.IsNullOrEmpty(brands) && minPrice == 0 && maxPrice > 0)
                        {
                            products = await DataControl.GetProductsUrlMaxPrice(url, maxPrice);
                            PageCount = Convert.ToInt32(Math.Ceiling((float)products.Count / 8));
                        }

                        else if (string.IsNullOrEmpty(brands) && minPrice > 0 && maxPrice == 0)
                        {
                            products = await DataControl.GetProductsUrlMinPrice(url, minPrice);
                            PageCount = Convert.ToInt32(Math.Ceiling((float)products.Count / 8));
                        }

                        else if (string.IsNullOrEmpty(brands) && minPrice > 0 && maxPrice > 0)
                        {
                            products = await DataControl.GetProductsUrlMinMaxPrice(url, minPrice, maxPrice);
                            PageCount = Convert.ToInt32(Math.Ceiling((float)products.Count / 8));
                        }

                        else if (!string.IsNullOrEmpty(brands) && minPrice == 0 && maxPrice == 0)
                        {
                            products = await DataControl.GetProductsUrlBrands(url, brands);
                            PageCount = Convert.ToInt32(Math.Ceiling((float)products.Count / 8));
                        }

                        else if (!string.IsNullOrEmpty(brands) && minPrice == 0 && maxPrice > 0)
                        {
                            products = await DataControl.GetProductsUrlBrandsMaxPrice(url, brands, maxPrice);
                            PageCount = Convert.ToInt32(Math.Ceiling((float)products.Count / 8));
                        }

                        else if (!string.IsNullOrEmpty(brands) && minPrice > 0 && maxPrice == 0)
                        {
                            products = await DataControl.GetProductsUrlBrandsMinPrice(url, brands, minPrice);
                            PageCount = Convert.ToInt32(Math.Ceiling((float)products.Count / 8));
                        }

                        else
                        {
                            products = await DataControl.GetProductsUrlBrandsMinMaxPrice(url, brands, minPrice, maxPrice);
                            PageCount = Convert.ToInt32(Math.Ceiling((float)products.Count / 8));
                        }
                    }

                        <input type="hidden" id="PageCount" value="@PageCount" />

                    if(PageCount > 1)
                    {
                            <li class="page-item">
                                <a class="page-link" id="btn-previous" aria-label="Previous">
                                    <span aria-hidden="true">&laquo;</span>
                                </a>
                            </li>
                    }

                    for (int i = 1; i <= PageCount; i++)
                    {
                        if (i <= 3)
                        {
                                <li class="page-item @(i == 1 ? "active" : "")" id="page-item-@i"><a class="page-link" data-pageindex="@i">@i</a></li>
                        }

                        else
                        {
                                <li class="page-item visually-hidden" id="page-item-@i"><a class="page-link" data-pageindex="@i">@i</a></li>
                        }
                    }

                    if (PageCount > 1)
                    {
                            <li class="page-item">
                                <a class="page-link" id="btn-next" aria-label="Next">
                                    <span aria-hidden="true">&raquo;</span>
                                </a>
                            </li>
                    }
                }
            </ul>
        </nav>
    </div>
</div>

@section Scripts {
    <script type="text/javascript">
        $(document).ready(function () {
            let searchParams = new URLSearchParams(window.location.search);
            let qParam = searchParams.get('q');
            $("#search-text").val(qParam);
            let pageIndex = searchParams.get('PageIndex');

            if (pageIndex != null && pageIndex != 1) {
                $("#page-item-" + pageIndex).addClass("active");
                $("#page-item-" + (pageIndex - 1)).removeClass("active");
                $("#page-item-" + (pageIndex + 1)).removeClass("active");

                if (parseInt($("#PageCount").val()) >= 3) {
                    if (pageIndex >= 3) {
                        if ($("#page-item-" + (parseInt(pageIndex) + 1)).length > 0) {
                            $("#page-item-" + parseInt(pageIndex)).removeClass("visually-hidden");
                            $("#page-item-" + (parseInt(pageIndex) + 1)).removeClass("visually-hidden");

                            for (let i = parseInt(pageIndex) - 2; i >= 1; i--) {
                                $("#page-item-" + i).hide();
                            }
                        }

                        else {
                            $("#btn-next").hide();
                            $("#page-item-" + parseInt(pageIndex)).addClass("active");

                            for (let i = parseInt(pageIndex) - 3; i >= 1; i--) {
                                $("#page-item-" + i).hide();
                            }

                            for (let i = parseInt(pageIndex); i >= parseInt(pageIndex) - 2; i--) {
                                if ($("#page-item-" + i).hasClass("visually-hidden")) {
                                    $("#page-item-" + i).removeClass("visually-hidden");
                                }
                            }
                        }
                    }
                }

                else {
                    if (pageIndex == 2) {
                        $("#btn-next").hide();
                    }
                }
            }

            else {
                $($(".pagination .page-item")[1]).addClass("active");
                $("#btn-previous").hide();
            }
        });

        $(document).on("mouseenter", ".prdCards", function () {
            var cardID = $(this).data("cardid");
            $('#btnBasket_' + cardID).css('display', 'block');
        });

        $(document).on("mouseleave", ".prdCards", function () {
            var cardID = $(this).data("cardid");
            $('#btnBasket_' + cardID).css('display', 'none');
        });

        $(document).on("click", ".prdCards", function () {
            var url = $(this).data("url");
            window.location.href = "/Products/Detail?url=" + url;
        });

        $(document).on("click", ".addBasket", function () {
            var productid = $(this).data("productid");
            let count = parseInt($("#basketProductCount").text()) + 1;
            $("#basketProductCount").text(count);
        });

        $(document).on("click", "#btnFilter", function () {
            let searchParams = new URLSearchParams(window.location.search);
            let url = searchParams.get('url');
            let q = searchParams.get('q');
            let checkedBrands = "";
            let minPrice = $("#minPrice").val();
            let maxPrice = $("#maxPrice").val();

            for (let i = 1; i <= $("input[name=brands]").length; i++) {
                if ($("#brand_" + i).is(":checked")) {
                    checkedBrands += i + ",";
                }
            }

            checkedBrands = checkedBrands.substring(0, checkedBrands.length - 1);

            if (url != null) {
                if (checkedBrands == "" && minPrice == 0 && maxPrice == 0) {
                    window.location.href = "/Home/Index?url=" + url + "&PageIndex=1";
                }

                else if (checkedBrands == "" && minPrice == 0 && maxPrice > 0) {
                    window.location.href = "/Home/Index?url=" + url + "&maxPrice=" + maxPrice + "&PageIndex=1";
                }

                else if (checkedBrands == "" && minPrice > 0 && maxPrice == 0) {
                    window.location.href = "/Home/Index?url=" + url + "&minPrice=" + minPrice + "&PageIndex=1";
                }

                else if (checkedBrands == "" && minPrice > 0 && maxPrice > 0) {
                    window.location.href = "/Home/Index?url=" + url + "&minPrice=" + minPrice + "&maxPrice=" + maxPrice + "&PageIndex=1";
                }

                else if (checkedBrands != "" && minPrice == 0 && maxPrice == 0) {
                    window.location.href = "/Home/Index?url=" + url + "&brands=" + checkedBrands + "&PageIndex=1";
                }

                else if (checkedBrands != "" && minPrice == 0 && maxPrice > 0) {
                    window.location.href = "/Home/Index?url=" + url + "&brands=" + checkedBrands + "&maxPrice=" + maxPrice + "&PageIndex=1";
                }

                else if (checkedBrands != "" && minPrice > 0 && maxPrice == 0) {
                    window.location.href = "/Home/Index?url=" + url + "&brands=" + checkedBrands + "&minPrice=" + minPrice + "&PageIndex=1";
                }
                
                else{
                    window.location.href = "/Home/Index?url=" + url + "&brands=" + checkedBrands + "&minPrice=" + minPrice + "&maxPrice=" + maxPrice + "&PageIndex=1";
                }
            }

            else {
                if (q != null) {
                    if (checkedBrands == "" && minPrice == 0 && maxPrice == 0) {
                        window.location.href = "/Home/Index?q=" + q + "&PageIndex=1";
                    }

                    else if (checkedBrands == "" && minPrice == 0 && maxPrice > 0) {
                        window.location.href = "/Home/Index?q=" + q + "&maxPrice=" + maxPrice + "&PageIndex=1";
                    }

                    else if (checkedBrands == "" && minPrice > 0 && maxPrice == 0) {
                        window.location.href = "/Home/Index?q=" + q + "&minPrice=" + minPrice + "&PageIndex=1";
                    }

                    else if (checkedBrands == "" && minPrice > 0 && maxPrice > 0) {
                        window.location.href = "/Home/Index?q=" + q + "&minPrice=" + minPrice + "&maxPrice=" + maxPrice + "&PageIndex=1";
                    }

                    else if (checkedBrands != "" && minPrice == 0 && maxPrice == 0) {
                        window.location.href = "/Home/Index?q=" + q + "&brands=" + checkedBrands + "&PageIndex=1";
                    }

                    else if (checkedBrands != "" && minPrice == 0 && maxPrice > 0) {
                        window.location.href = "/Home/Index?q=" + q + "&brands=" + checkedBrands + "&maxPrice=" + maxPrice + "&PageIndex=1";
                    }

                    else if (checkedBrands != "" && minPrice > 0 && maxPrice == 0) {
                        window.location.href = "/Home/Index?q=" + q + "&brands=" + checkedBrands + "&minPrice=" + minPrice + "&PageIndex=1";
                    }

                    else {
                        window.location.href = "/Home/Index?q=" + q + "&brands=" + checkedBrands + "&minPrice=" + minPrice + "&maxPrice=" + maxPrice + "&PageIndex=1";
                    }
                }

                else {
                    if (checkedBrands == "" && minPrice == 0 && maxPrice == 0) {
                        window.location.href = "/Home/Index?PageIndex=1";
                    }

                    else if (checkedBrands == "" && minPrice == 0 && maxPrice > 0) {
                        window.location.href = "/Home/Index?maxPrice=" + maxPrice + "&PageIndex=1";
                    }

                    else if (checkedBrands == "" && minPrice > 0 && maxPrice == 0) {
                        window.location.href = "/Home/Index?minPrice=" + minPrice + "&PageIndex=1";
                    }

                    else if (checkedBrands == "" && minPrice > 0 && maxPrice > 0) {
                        window.location.href = "/Home/Index?minPrice=" + minPrice + "&maxPrice=" + maxPrice + "&PageIndex=1";
                    }

                    else if (checkedBrands != "" && minPrice == 0 && maxPrice == 0) {
                        window.location.href = "/Home/Index?brands=" + checkedBrands + "&PageIndex=1";
                    }

                    else if (checkedBrands != "" && minPrice == 0 && maxPrice > 0) {
                        window.location.href = "/Home/Index?brands=" + checkedBrands + "&maxPrice=" + maxPrice + "&PageIndex=1";
                    }

                    else if (checkedBrands != "" && minPrice > 0 && maxPrice == 0) {
                        window.location.href = "/Home/Index?brands=" + checkedBrands + "&minPrice=" + minPrice + "&PageIndex=1";
                    }

                    else {
                        window.location.href = "/Home/Index?brands=" + checkedBrands + "&minPrice=" + minPrice + "&maxPrice=" + maxPrice + "&PageIndex=1";
                    }
                }
            }
        });

        $(document).on("keyup", "#search-text", function (e) {
            if (e.keyCode == 8) {
                $(this).val().length -= 1;
            }

            if ($(this).val().length >= 2) {
                if (e.keyCode == 13) {
                    window.location.href = "/Home/Index?q=" + $("#search-text").val() + "&PageIndex=1";
                }

                else {
                    ChangeSearchDatas($(this).val());
                }
            }

            else {
                $("#search-panel").text("");
                $("#search-panel").append(`
                    <p id="search-empty">Aramaya başlamak için <strong>en az 2 karakter</strong> yazmalısınız.</p>
                `);
            }
        });

        $(document).on("search", '#search-text', function () {
            $("#search-panel").text("");
            $("#search-panel").append(`
                <p id="search-empty">Aramaya başlamak için <strong>en az 2 karakter</strong> yazmalısınız.</p>
            `);
        });

        $(document).on("click", '.page-link', function () {
            let searchParams = new URLSearchParams(window.location.search);
            let url = searchParams.get('url');
            let q = searchParams.get('q');
            let pageindex = $(this).data("pageindex");
            let checkedBrands = "";
            let minPrice = $("#minPrice").val();
            let maxPrice = $("#maxPrice").val();

            for (let i = 1; i <= $("input[name=brands]").length; i++) {
                if ($("#brand_" + i).is(":checked")) {
                    checkedBrands += i + ",";
                }
            }

            checkedBrands = checkedBrands.substring(0, checkedBrands.length - 1);

            if (url != null) {
                if (checkedBrands == "" && minPrice == 0 && maxPrice == 0) {
                    window.location.href = "/Home/Index?url=" + url + "&PageIndex=" + pageindex;
                }

                else if (checkedBrands == "" && minPrice == 0 && maxPrice > 0) {
                    window.location.href = "/Home/Index?url=" + url + "&maxPrice=" + maxPrice + "&PageIndex=" + pageindex;
                }

                else if (checkedBrands == "" && minPrice > 0 && maxPrice == 0) {
                    window.location.href = "/Home/Index?url=" + url + "&minPrice=" + minPrice + "&PageIndex=" + pageindex;
                }

                else if (checkedBrands == "" && minPrice > 0 && maxPrice > 0) {
                    window.location.href = "/Home/Index?url=" + url + "&minPrice=" + minPrice + "&maxPrice=" + maxPrice + "&PageIndex=" + pageindex;
                }

                else if (checkedBrands != "" && minPrice == 0 && maxPrice == 0) {
                    window.location.href = "/Home/Index?url=" + url + "&brands=" + checkedBrands + "&PageIndex=" + pageindex;
                }

                else if (checkedBrands != "" && minPrice == 0 && maxPrice > 0) {
                    window.location.href = "/Home/Index?url=" + url + "&brands=" + checkedBrands + "&maxPrice=" + maxPrice + "&PageIndex=" + pageindex;
                }

                else if (checkedBrands != "" && minPrice > 0 && maxPrice == 0) {
                    window.location.href = "/Home/Index?url=" + url + "&brands=" + checkedBrands + "&minPrice=" + minPrice + "&PageIndex=" + pageindex;
                }

                else {
                    window.location.href = "/Home/Index?url=" + url + "&brands=" + checkedBrands + "&minPrice=" + minPrice + "&maxPrice=" + maxPrice + "&PageIndex=" + pageindex;
                }
            }

            else {
                if (q != null) {
                    if (checkedBrands == "" && minPrice == 0 && maxPrice == 0) {
                        window.location.href = "/Home/Index?q=" + q + "&PageIndex=" + pageindex;
                    }

                    else if (checkedBrands == "" && minPrice == 0 && maxPrice > 0) {
                        window.location.href = "/Home/Index?q=" + q + "&maxPrice=" + maxPrice + "&PageIndex=" + pageindex;
                    }

                    else if (checkedBrands == "" && minPrice > 0 && maxPrice == 0) {
                        window.location.href = "/Home/Index?q=" + q + "&minPrice=" + minPrice + "&PageIndex=" + pageindex;
                    }

                    else if (checkedBrands == "" && minPrice > 0 && maxPrice > 0) {
                        window.location.href = "/Home/Index?q=" + q + "&minPrice=" + minPrice + "&maxPrice=" + maxPrice + "&PageIndex=" + pageindex;
                    }

                    else if (checkedBrands != "" && minPrice == 0 && maxPrice == 0) {
                        window.location.href = "/Home/Index?q=" + q + "&brands=" + checkedBrands + "&PageIndex=" + pageindex;
                    }

                    else if (checkedBrands != "" && minPrice == 0 && maxPrice > 0) {
                        window.location.href = "/Home/Index?q=" + q + "&brands=" + checkedBrands + "&maxPrice=" + maxPrice + "&PageIndex=" + pageindex;
                    }

                    else if (checkedBrands != "" && minPrice > 0 && maxPrice == 0) {
                        window.location.href = "/Home/Index?q=" + q + "&brands=" + checkedBrands + "&minPrice=" + minPrice + "&PageIndex=" + pageindex;
                    }

                    else {
                        window.location.href = "/Home/Index?q=" + q + "&brands=" + checkedBrands + "&minPrice=" + minPrice + "&maxPrice=" + maxPrice + "&PageIndex=" + pageindex;
                    }
                }

                else {
                    if (checkedBrands == "" && minPrice == 0 && maxPrice == 0) {
                        window.location.href = "/Home/Index?PageIndex=" + pageindex;
                    }

                    else if (checkedBrands == "" && minPrice == 0 && maxPrice > 0) {
                        window.location.href = "/Home/Index?maxPrice=" + maxPrice + "&PageIndex=" + pageindex;
                    }

                    else if (checkedBrands == "" && minPrice > 0 && maxPrice == 0) {
                        window.location.href = "/Home/Index?minPrice=" + minPrice + "&PageIndex=" + pageindex;
                    }

                    else if (checkedBrands == "" && minPrice > 0 && maxPrice > 0) {
                        window.location.href = "/Home/Index?minPrice=" + minPrice + "&maxPrice=" + maxPrice + "&PageIndex=" + pageindex;
                    }

                    else if (checkedBrands != "" && minPrice == 0 && maxPrice == 0) {
                        window.location.href = "/Home/Index?brands=" + checkedBrands + "&PageIndex=" + pageindex;
                    }

                    else if (checkedBrands != "" && minPrice == 0 && maxPrice > 0) {
                        window.location.href = "/Home/Index?brands=" + checkedBrands + "&maxPrice=" + maxPrice + "&PageIndex=" + pageindex;
                    }

                    else if (checkedBrands != "" && minPrice > 0 && maxPrice == 0) {
                        window.location.href = "/Home/Index?brands=" + checkedBrands + "&minPrice=" + minPrice + "&PageIndex=" + pageindex;
                    }

                    else {
                        window.location.href = "/Home/Index?brands=" + checkedBrands + "&minPrice=" + minPrice + "&maxPrice=" + maxPrice + "&PageIndex=" + pageindex;
                    }
                }
            }
        });

        $(document).on("click", '#btn-previous', function () {
            let searchParams = new URLSearchParams(window.location.search);
            let pageIndex = searchParams.get('PageIndex');
            pageIndex = parseInt(pageIndex) - 1;
            let url = searchParams.get('url');
            let q = searchParams.get('q');
            let checkedBrands = "";
            let minPrice = $("#minPrice").val();
            let maxPrice = $("#maxPrice").val();

            for (let i = 1; i <= $("input[name=brands]").length; i++) {
                if ($("#brand_" + i).is(":checked")) {
                    checkedBrands += i + ",";
                }
            }

            checkedBrands = checkedBrands.substring(0, checkedBrands.length - 1);

            if (pageIndex >= 1) {
                if (url != null) {
                    if (checkedBrands == "" && minPrice == 0 && maxPrice == 0) {
                        window.location.href = "/Home/Index?url=" + url + "&PageIndex=" + pageIndex;
                    }

                    else if (checkedBrands == "" && minPrice == 0 && maxPrice > 0) {
                        window.location.href = "/Home/Index?url=" + url + "&maxPrice=" + maxPrice + "&PageIndex=" + pageIndex;
                    }

                    else if (checkedBrands == "" && minPrice > 0 && maxPrice == 0) {
                        window.location.href = "/Home/Index?url=" + url + "&minPrice=" + minPrice + "&PageIndex=" + pageIndex;
                    }

                    else if (checkedBrands == "" && minPrice > 0 && maxPrice > 0) {
                        window.location.href = "/Home/Index?url=" + url + "&minPrice=" + minPrice + "&maxPrice=" + maxPrice + "&PageIndex=" + pageIndex;
                    }

                    else if (checkedBrands != "" && minPrice == 0 && maxPrice == 0) {
                        window.location.href = "/Home/Index?url=" + url + "&brands=" + checkedBrands + "&PageIndex=" + pageIndex;
                    }

                    else if (checkedBrands != "" && minPrice == 0 && maxPrice > 0) {
                        window.location.href = "/Home/Index?url=" + url + "&brands=" + checkedBrands + "&maxPrice=" + maxPrice + "&PageIndex=" + pageIndex;
                    }

                    else if (checkedBrands != "" && minPrice > 0 && maxPrice == 0) {
                        window.location.href = "/Home/Index?url=" + url + "&brands=" + checkedBrands + "&minPrice=" + minPrice + "&PageIndex=" + pageIndex;
                    }

                    else {
                        window.location.href = "/Home/Index?url=" + url + "&brands=" + checkedBrands + "&minPrice=" + minPrice + "&maxPrice=" + maxPrice + "&PageIndex=" + pageIndex;
                    }
                }

                else {
                    if (q != null) {
                        if (checkedBrands == "" && minPrice == 0 && maxPrice == 0) {
                            window.location.href = "/Home/Index?q=" + q + "&PageIndex=" + pageIndex;
                        }

                        else if (checkedBrands == "" && minPrice == 0 && maxPrice > 0) {
                            window.location.href = "/Home/Index?q=" + q + "&maxPrice=" + maxPrice + "&PageIndex=" + pageIndex;
                        }

                        else if (checkedBrands == "" && minPrice > 0 && maxPrice == 0) {
                            window.location.href = "/Home/Index?q=" + q + "&minPrice=" + minPrice + "&PageIndex=" + pageIndex;
                        }

                        else if (checkedBrands == "" && minPrice > 0 && maxPrice > 0) {
                            window.location.href = "/Home/Index?q=" + q + "&minPrice=" + minPrice + "&maxPrice=" + maxPrice + "&PageIndex=" + pageIndex;
                        }

                        else if (checkedBrands != "" && minPrice == 0 && maxPrice == 0) {
                            window.location.href = "/Home/Index?q=" + q + "&brands=" + checkedBrands + "&PageIndex=" + pageIndex;
                        }

                        else if (checkedBrands != "" && minPrice == 0 && maxPrice > 0) {
                            window.location.href = "/Home/Index?q=" + q + "&brands=" + checkedBrands + "&maxPrice=" + maxPrice + "&PageIndex=" + pageIndex;
                        }

                        else if (checkedBrands != "" && minPrice > 0 && maxPrice == 0) {
                            window.location.href = "/Home/Index?q=" + q + "&brands=" + checkedBrands + "&minPrice=" + minPrice + "&PageIndex=" + pageIndex;
                        }

                        else {
                            window.location.href = "/Home/Index?q=" + q + "&brands=" + checkedBrands + "&minPrice=" + minPrice + "&maxPrice=" + maxPrice + "&PageIndex=" + pageIndex;
                        }
                    }

                    else {
                        if (checkedBrands == "" && minPrice == 0 && maxPrice == 0) {
                            window.location.href = "/Home/Index?PageIndex=" + pageIndex;
                        }

                        else if (checkedBrands == "" && minPrice == 0 && maxPrice > 0) {
                            window.location.href = "/Home/Index?maxPrice=" + maxPrice + "&PageIndex=" + pageIndex;
                        }

                        else if (checkedBrands == "" && minPrice > 0 && maxPrice == 0) {
                            window.location.href = "/Home/Index?minPrice=" + minPrice + "&PageIndex=" + pageIndex;
                        }

                        else if (checkedBrands == "" && minPrice > 0 && maxPrice > 0) {
                            window.location.href = "/Home/Index?minPrice=" + minPrice + "&maxPrice=" + maxPrice + "&PageIndex=" + pageIndex;
                        }

                        else if (checkedBrands != "" && minPrice == 0 && maxPrice == 0) {
                            window.location.href = "/Home/Index?brands=" + checkedBrands + "&PageIndex=" + pageIndex;
                        }

                        else if (checkedBrands != "" && minPrice == 0 && maxPrice > 0) {
                            window.location.href = "/Home/Index?brands=" + checkedBrands + "&maxPrice=" + maxPrice + "&PageIndex=" + pageIndex;
                        }

                        else if (checkedBrands != "" && minPrice > 0 && maxPrice == 0) {
                            window.location.href = "/Home/Index?brands=" + checkedBrands + "&minPrice=" + minPrice + "&PageIndex=" + pageIndex;
                        }

                        else {
                            window.location.href = "/Home/Index?brands=" + checkedBrands + "&minPrice=" + minPrice + "&maxPrice=" + maxPrice + "&PageIndex=" + pageIndex;
                        }
                    }
                }
            }
        });

        $(document).on("click", '#btn-next', function () {
            let searchParams = new URLSearchParams(window.location.search);
            let pageIndex = searchParams.get('PageIndex');
            let url = searchParams.get('url');
            let q = searchParams.get('q');
            let checkedBrands = "";
            let minPrice = $("#minPrice").val();
            let maxPrice = $("#maxPrice").val();

            for (let i = 1; i <= $("input[name=brands]").length; i++) {
                if ($("#brand_" + i).is(":checked")) {
                    checkedBrands += i + ",";
                }
            }

            checkedBrands = checkedBrands.substring(0, checkedBrands.length - 1);

            if (url == null) {
                if (pageIndex == null) {
                    pageIndex = 1;
                }

                pageIndex = parseInt(pageIndex) + 1;

                if (q != null) {
                    if (checkedBrands == "" && minPrice == 0 && maxPrice == 0) {
                        window.location.href = "/Home/Index?q=" + q + "&PageIndex=" + pageIndex;
                    }

                    else if (checkedBrands == "" && minPrice == 0 && maxPrice > 0) {
                        window.location.href = "/Home/Index?q=" + q + "&maxPrice=" + maxPrice + "&PageIndex=" + pageIndex;
                    }

                    else if (checkedBrands == "" && minPrice > 0 && maxPrice == 0) {
                        window.location.href = "/Home/Index?q=" + q + "&minPrice=" + minPrice + "&PageIndex=" + pageIndex;
                    }

                    else if (checkedBrands == "" && minPrice > 0 && maxPrice > 0) {
                        window.location.href = "/Home/Index?q=" + q + "&minPrice=" + minPrice + "&maxPrice=" + maxPrice + "&PageIndex=" + pageIndex;
                    }

                    else if (checkedBrands != "" && minPrice == 0 && maxPrice == 0) {
                        window.location.href = "/Home/Index?q=" + q + "&brands=" + checkedBrands + "&PageIndex=" + pageIndex;
                    }

                    else if (checkedBrands != "" && minPrice == 0 && maxPrice > 0) {
                        window.location.href = "/Home/Index?q=" + q + "&brands=" + checkedBrands + "&maxPrice=" + maxPrice + "&PageIndex=" + pageIndex;
                    }

                    else if (checkedBrands != "" && minPrice > 0 && maxPrice == 0) {
                        window.location.href = "/Home/Index?q=" + q + "&brands=" + checkedBrands + "&minPrice=" + minPrice + "&PageIndex=" + pageIndex;
                    }

                    else {
                        window.location.href = "/Home/Index?q=" + q + "&brands=" + checkedBrands + "&minPrice=" + minPrice + "&maxPrice=" + maxPrice + "&PageIndex=" + pageIndex;
                    }
                }

                else {
                    if (checkedBrands == "" && minPrice == 0 && maxPrice == 0) {
                        window.location.href = "/Home/Index?PageIndex=" + pageIndex;
                    }

                    else if (checkedBrands == "" && minPrice == 0 && maxPrice > 0) {
                        window.location.href = "/Home/Index?maxPrice=" + maxPrice + "&PageIndex=" + pageIndex;
                    }

                    else if (checkedBrands == "" && minPrice > 0 && maxPrice == 0) {
                        window.location.href = "/Home/Index?minPrice=" + minPrice + "&PageIndex=" + pageIndex;
                    }

                    else if (checkedBrands == "" && minPrice > 0 && maxPrice > 0) {
                        window.location.href = "/Home/Index?minPrice=" + minPrice + "&maxPrice=" + maxPrice + "&PageIndex=" + pageIndex;
                    }

                    else if (checkedBrands != "" && minPrice == 0 && maxPrice == 0) {
                        window.location.href = "/Home/Index?brands=" + checkedBrands + "&PageIndex=" + pageIndex;
                    }

                    else if (checkedBrands != "" && minPrice == 0 && maxPrice > 0) {
                        window.location.href = "/Home/Index?brands=" + checkedBrands + "&maxPrice=" + maxPrice + "&PageIndex=" + pageIndex;
                    }

                    else if (checkedBrands != "" && minPrice > 0 && maxPrice == 0) {
                        window.location.href = "/Home/Index?brands=" + checkedBrands + "&minPrice=" + minPrice + "&PageIndex=" + pageIndex;
                    }

                    else {
                        window.location.href = "/Home/Index?brands=" + checkedBrands + "&minPrice=" + minPrice + "&maxPrice=" + maxPrice + "&PageIndex=" + pageIndex;
                    }
                }
            }

            else {
                pageIndex = parseInt(pageIndex) + 1;

                if (checkedBrands == "" && minPrice == 0 && maxPrice == 0) {
                    window.location.href = "/Home/Index?url=" + url + "&PageIndex=" + pageIndex;
                }

                else if (checkedBrands == "" && minPrice == 0 && maxPrice > 0) {
                    window.location.href = "/Home/Index?url=" + url + "&maxPrice=" + maxPrice + "&PageIndex=" + pageIndex;
                }

                else if (checkedBrands == "" && minPrice > 0 && maxPrice == 0) {
                    window.location.href = "/Home/Index?url=" + url + "&minPrice=" + minPrice + "&PageIndex=" + pageIndex;
                }

                else if (checkedBrands == "" && minPrice > 0 && maxPrice > 0) {
                    window.location.href = "/Home/Index?url=" + url + "&minPrice=" + minPrice + "&maxPrice=" + maxPrice + "&PageIndex=" + pageIndex;
                }

                else if (checkedBrands != "" && minPrice == 0 && maxPrice == 0) {
                    window.location.href = "/Home/Index?url=" + url + "&brands=" + checkedBrands + "&PageIndex=" + pageIndex;
                }

                else if (checkedBrands != "" && minPrice == 0 && maxPrice > 0) {
                    window.location.href = "/Home/Index?url=" + url + "&brands=" + checkedBrands + "&maxPrice=" + maxPrice + "&PageIndex=" + pageIndex;
                }

                else if (checkedBrands != "" && minPrice > 0 && maxPrice == 0) {
                    window.location.href = "/Home/Index?url=" + url + "&brands=" + checkedBrands + "&minPrice=" + minPrice + "&PageIndex=" + pageIndex;
                }

                else {
                    window.location.href = "/Home/Index?url=" + url + "&brands=" + checkedBrands + "&minPrice=" + minPrice + "&maxPrice=" + maxPrice + "&PageIndex=" + pageIndex;
                }
            }
        });
    </script>
}